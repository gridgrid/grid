describe('grid-core', function () {
    var grid;
    beforeEach(inject(function () {
        grid = require('@grid/grid-core')();
    }));

    it('should be able to add and get back a col object', function () {
    });

   
});