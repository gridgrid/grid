machine:
  node:
    version: 6.3.0
dependencies:
  pre:
    - npm install -g npm@5.x.x
test:
  override:
    - npm run build
deployment:
  master:
    branch: [master, next]
    commands:
      - npm run semantic-release
    
